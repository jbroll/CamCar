# Copy to .env (untracked) and fill in your network. `make build` bakes these
# into src/gen/secrets.h, which seeds NVS on first boot. Leave blank/absent to
# boot straight into SoftAP setup mode instead.
WIFI_SSID=
WIFI_PASSWORD=
